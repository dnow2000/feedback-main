from sqlalchemy_api_handler import logger


def create_user_scenes():
    logger.info('create_user_scenes')

    user_scenes = []

    logger.info('created {} article_users_by_name'.format(len(user_scenes)))
