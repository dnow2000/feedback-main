'use strict';

var _index = require('../index');

var reduxThunkApi = _interopRequireWildcard(_index);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

jest.mock('fetch-normalize-data', function () {
  return {
    API_ERROR: 'API_ERROR',
    SERVER_ERROR: 'SERVER_ERROR'
  };
});

describe('redux-thunk-data | Exports constants', function () {
  it('should ', function () {
    expect(reduxThunkApi.API_ERROR).toBeDefined();
    expect(reduxThunkApi.SERVER_ERROR).toBeDefined();
  });
});