"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getProcessedDatum = getProcessedDatum;
exports.getProcessedData = getProcessedData;
exports["default"] = void 0;

var _lodash = _interopRequireDefault(require("lodash.uniqby"));

var _utils = require("./utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function getProcessedDatum(datum, index, config) {
  var apiPath = config.apiPath,
      data = config.data,
      resolve = config.resolve;
  var activityTag = config.activityTag || apiPath;
  var getDatumIdKey = config.getDatumIdKey || _utils.getDefaultDatumIdKey;
  var getDatumIdValue = config.getDatumIdValue || _utils.getDefaultDatumIdValue;
  var processedDatum = Object.assign(_defineProperty({}, getDatumIdKey(datum), getDatumIdValue(datum, index)), datum);

  if (activityTag) {
    if (!processedDatum.__ACTIVITIES__) {
      processedDatum.__ACTIVITIES__ = [activityTag];
    } else {
      processedDatum.__ACTIVITIES__.push(activityTag);
    }
  }

  if (resolve) {
    processedDatum = config.resolve(processedDatum, data, config);
  }

  return processedDatum;
}

function getProcessedData(data, config) {
  var getDatumIdValue = config.getDatumIdValue || _utils.getDefaultDatumIdValue;
  var unifyConfig = Object.assign({
    data: data
  }, config);
  var processedData = data.map(function (datum, index) {
    return getProcessedDatum(datum, index, unifyConfig);
  });
  return (0, _lodash["default"])(processedData, getDatumIdValue);
}

var _default = getProcessedData;
exports["default"] = _default;